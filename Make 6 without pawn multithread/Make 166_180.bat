feg -b KQQQKB			
				if errorlevel 1 goto abort
feg -b KQQQKR			
				if errorlevel 1 goto abort
feg -b KQQRKB			
				if errorlevel 1 goto abort
feg -b KQQRKN			
				if errorlevel 1 goto abort
feg -b KQQRKR			
				if errorlevel 1 goto abort
feg -b KQQRKQ			
				if errorlevel 1 goto abort
feg -b KQQBKQ			
				if errorlevel 1 goto abort
feg -b KQQBKN			
				if errorlevel 1 goto abort
feg -b KQQBKB			
				if errorlevel 1 goto abort
feg -b KQQNKB			
				if errorlevel 1 goto abort
feg -b KQQNKQ			
				if errorlevel 1 goto abort
feg -b KQQBKR			
				if errorlevel 1 goto abort
feg -b KQQNKR			
				if errorlevel 1 goto abort
feg -b KQQNKN			
				if errorlevel 1 goto abort
feg -b KQRRKQ			
				if errorlevel 1 goto abort
