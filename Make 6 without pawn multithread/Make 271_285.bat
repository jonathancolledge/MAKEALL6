feg -b KQKRRB			
				if errorlevel 1 goto abort
feg -b KNKRBB			
				if errorlevel 1 goto abort
feg -b KNKRBN			
				if errorlevel 1 goto abort
feg -b KBKRBN			
				if errorlevel 1 goto abort
feg -b KQKRRN			
				if errorlevel 1 goto abort
feg -b KRKRBN			
				if errorlevel 1 goto abort
feg -b KNKRNN			
				if errorlevel 1 goto abort
feg -b KBKRNN			
				if errorlevel 1 goto abort
feg -b KRKRNN			
				if errorlevel 1 goto abort
feg -b KQKRBB			
				if errorlevel 1 goto abort
feg -b KNKBBB			
				if errorlevel 1 goto abort
feg -b KBKBBB			
				if errorlevel 1 goto abort
feg -b KRKBBB			
				if errorlevel 1 goto abort
feg -b KQKRBN			
				if errorlevel 1 goto abort
feg -b KRKBBN			
				if errorlevel 1 goto abort
