feg -b KRRRRK			
				if errorlevel 1 goto abort
feg -b KQNNNK			
				if errorlevel 1 goto abort
feg -b KRRNNK			
				if errorlevel 1 goto abort
feg -b KRRBBK			
				if errorlevel 1 goto abort
feg -b KRRBNK			
				if errorlevel 1 goto abort
feg -b KRBBBK			
				if errorlevel 1 goto abort
feg -b KBBBNK			
				if errorlevel 1 goto abort
feg -b KRBBNK			
				if errorlevel 1 goto abort
feg -b KRBNNK			
				if errorlevel 1 goto abort
feg -b KBBNNK			
				if errorlevel 1 goto abort
feg -b KBBBBK			
				if errorlevel 1 goto abort
feg -b KRNNNK			
				if errorlevel 1 goto abort
feg -b KBNNNK			
				if errorlevel 1 goto abort
feg -b KNNNNK			
				if errorlevel 1 goto abort
feg -b KQQQKN			
				if errorlevel 1 goto abort
